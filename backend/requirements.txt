# Core
python-dotenv>=1.0.1
pydantic>=2.7.0
PyYAML>=6.0.1
tqdm>=4.66.0
rich>=13.7.0

# LangChain + LLM
langchain>=0.2.16
langchain-core>=0.2.38
langchain-google-genai>=1.0.10
langchain-huggingface>=0.0.3
transformers>=4.43.0
accelerate>=0.33.0
sentence-transformers>=3.0.1

# Neo4j
neo4j>=5.22.0

# Graph / clustering
networkx>=3.3
numpy>=1.26.0
scikit-learn>=1.4.0

# Optional GraphRAG-like hierarchical Leiden support.
# On Windows, this may fail depending on your Python/build tools. The code has fallback clustering.
graspologic>=3.4.1; platform_system != "Windows"

# Optional OCR. Install separately if you need PDF -> Markdown OCR.
# marker-pdf

# API server for frontend integration
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
